#include <stdio.h>
/**
 * main - prints a new and the name of the program
 * @argc: The number of arguments in the program
 * @argv: Array of pointers to the arguments
 *
 * Return: Always 0
 */
int main(int __attribute__((__unused__)) argc, char *argv[])
{
	printf("%s\n", argv[0]);
	
	return (0);
}
