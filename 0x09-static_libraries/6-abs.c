#include "main.h"
/**
 * _abs - computes the absolute integer value
 * @b: - is the integer to be compiled
 *
 * Return: Always returns 0
 */
int _abs(int b)
{
	if (b >= 0)
		return (b);
	else
		return (-b);
}
